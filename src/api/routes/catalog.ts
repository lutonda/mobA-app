var express = require('express');
var router = express.Router();

/*let currency = require('../controllers/currencyController');

router.get('/:id', currency.getOne);
router.get('/', currency.getAll);

/*
*-/
router.get('/:id', currency.getOne);
router.get('/', currency.getAll);*/

module.exports = router ;